# Changelog

Formato [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Entradas nuevas arriba.

## 2026.8.19 — [Jose]

### Added
- `CODEMAP.md` — primer mapa técnico del workspace: documenta `LogsSpec`/`TGroup` de
  `modules/status/status.ts` y `StatusConfig` de `modules/utiles/config.ts`, el desajuste entre el
  nombre del directorio (`status-base`) y el nombre real del paquete (`logs-status-base`), y su
  único consumidor conocido (`services/logs-web`).
